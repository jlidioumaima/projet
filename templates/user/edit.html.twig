{% extends 'base.html.twig' %}

{% block titlepage %}Edit User{% endblock %}

{% block content %}
  <div class="content">

   <div class="card-header header-elements-inline" style="background-color:Teal">
						<h3 class="card-title" style="color:white">Modifier Mon Profile :{{user.nom}}</h3>
						<div class="header-elements">
							 <a class="btn btn-dark" style="width:160px" href="{{ path('app_user_index') }}"><i class="icon-reply"></i>Liste des Utilisateurs </a>
	                	</div>
					</div>
					<br />
   	<div class="card">
							
							<div class="card-body">
    {{ include('user/_form.html.twig', {'button_label': 'Modifier'}) }}
  
      


{% endblock %}
