<form method="post" action="{{ path('app_voyage_organiser_delete', {'id': voyage_organiser.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: contents;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voyage_organiser.id) }}">
      <button class="btn btn-danger"  style="width:40px"><i class="icon-trash-alt"></i>  </button>
</form>