{% extends 'base.html.twig' %}

{% block titlepage %}Edit User{% endblock %}

{% block content %}
  <div class="content">

   <div class="card-header header-elements-inline" style="background-color:Teal">
						<h3 class="card-title" style="color:white">Modifier {{site.nom}}</h3>
						<div class="header-elements">
							 <a class="btn btn-info" style="width:50px" href="{{ path('app_sites_index') }}"><i class="icon-reply"></i> </a>
	                	</div>
					</div>
					<br />
   	<div class="card">
							
							<div class="card-body">

    {{ include('sites/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_sites_index') }}">back to list</a>

    {{ include('sites/_delete_form.html.twig') }}
{% endblock %}
